interface:
  display_name: "PAIChecker"
  short_description: "Detect and classify PR-Issue misalignment"
  default_prompt: "Use $paichecker in types mode to analyze the supplied pull request and its linked issue evidence. Return JSON only."
